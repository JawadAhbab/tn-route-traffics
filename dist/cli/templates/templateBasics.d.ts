interface Props {
    site: string;
    cdn?: string;
    cdnaccess?: string;
    loggerImport?: string;
    loggerMethod?: string;
}
export declare const templateBasics: ({ site, cdn, cdnaccess, loggerImport, loggerMethod }: Props) => string;
export {};
